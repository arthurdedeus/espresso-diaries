---
import { recipe } from '../lib/coffee';

interface Props {
  dose: number;
  yieldAmount: number;
  time?: number;
  yieldUnit?: 'g' | 'ml';
}

const { dose, yieldAmount, time, yieldUnit = 'g' } = Astro.props;
---
<p class="recipe-line">{recipe(dose, yieldAmount, time, yieldUnit)}</p>
